# Production Environment Configuration for STEPS Application
# Copy this file to your server and update the values accordingly

NODE_ENV=production

# API Configuration
# Update this to point to your production API server
NEXT_PUBLIC_BASE_URL=https://your-production-api-server.com

# Force real API calls (set to true for production)
NEXT_PUBLIC_FORCE_REAL_API=true

# Environment identifier
NEXT_PUBLIC_CO_DEV_ENV=production

# Add any other environment variables your application needs
# For example:
# NEXT_PUBLIC_APP_VERSION=1.0.0
# NEXT_PUBLIC_SUPPORT_EMAIL=support@yourcompany.com